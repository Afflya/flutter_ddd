name: flutter_ddd
description: Core tools for DDD
version: 0.0.1
homepage: https://github.com/Afflya/flutter_ddd

publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.6"

dependencies:
  dartz: '>=0.10.1'
  flutter:
    sdk: flutter
  uuid: '>=3.0.5'

dev_dependencies:
  flutter_lints: '>=2.0.2-0 <3.0.0'
  flutter_test:
    sdk: flutter

flutter:

